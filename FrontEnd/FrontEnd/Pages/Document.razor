@page "/document"
@inject IJSRuntime JS

<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Microsoft JhengHei', 'Segoe UI', sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: #333;
        overflow: hidden;
    }

    .presentation-container {
        width: 100vw;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
    }

    .slide {
        width: 90%;
        max-width: 1200px;
        height: 80vh;
        background: white;
        border-radius: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        padding: 60px;
        display: none;
        overflow-y: auto;
        position: relative;
    }

        .slide.active {
            display: block;
            animation: slideIn 0.5s ease-in-out;
        }

    @@keyframes slideIn {
        from {
            opacity: 0;
            transform: translateX(50px);
        }

        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .slide-header {
        text-align: center;
        margin-bottom: 40px;
        border-bottom: 3px solid #667eea;
        padding-bottom: 20px;
    }

    .slide-title {
        font-size: 2.5rem;
        color: #667eea;
        margin-bottom: 10px;
        font-weight: bold;
    }

    .slide-subtitle {
        font-size: 1.2rem;
        color: #666;
    }

    .content-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
        margin-bottom: 30px;
    }

    .feature-card {
        background: #f8f9ff;
        padding: 25px;
        border-radius: 15px;
        border-left: 5px solid #667eea;
        transition: transform 0.3s ease;
    }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.2);
        }

    .feature-title {
        font-size: 1.3rem;
        color: #667eea;
        margin-bottom: 15px;
        font-weight: bold;
    }

    .feature-list {
        list-style: none;
        padding: 0;
    }

        .feature-list li {
            padding: 8px 0;
            padding-left: 25px;
            position: relative;
        }

            .feature-list li:before {
                content: "✓";
                position: absolute;
                left: 0;
                color: #4CAF50;
                font-weight: bold;
                font-size: 1.2rem;
            }

    .tech-stack {
        display: flex;
        justify-content: space-around;
        margin: 30px 0;
    }

    .tech-item {
        text-align: center;
        padding: 20px;
        background: #f0f4ff;
        border-radius: 10px;
        min-width: 150px;
    }

    .tech-icon {
        font-size: 2.5rem;
        margin-bottom: 10px;
    }

    .flow-diagram {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 30px 0;
        padding: 20px;
        background: #f8f9ff;
        border-radius: 15px;
    }

    .flow-step {
        text-align: center;
        padding: 15px;
        background: white;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        min-width: 120px;
    }

    .flow-arrow {
        font-size: 1.5rem;
        color: #667eea;
        font-weight: bold;
    }

    .navigation {
        position: fixed;
        bottom: 30px;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 15px;
        z-index: 1001;
    }

    .nav-btn {
        padding: 12px 25px;
        background: rgba(255,255,255,0.9);
        border: none;
        border-radius: 25px;
        cursor: pointer;
        font-size: 1rem;
        font-weight: bold;
        color: #667eea;
        transition: all 0.3s ease;
        backdrop-filter: blur(10px);
    }

        .nav-btn:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

        .nav-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

    .slide-number {
        position: absolute;
        top: 20px;
        right: 30px;
        background: #667eea;
        color: white;
        padding: 8px 15px;
        border-radius: 20px;
        font-size: 0.9rem;
    }

    .logo {
        position: absolute;
        top: 20px;
        left: 30px;
        font-weight: bold;
        font-size: 1.2rem;
        color: #667eea;
    }

    .highlight-text {
        color: #e74c3c;
        font-weight: bold;
    }

    .validation-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 20px;
    }

    .validation-card {
        background: linear-gradient(135deg, #ff9a56 0%, #ff6b6b 100%);
        color: white;
        padding: 20px;
        border-radius: 15px;
        text-align: center;
    }

    .back-to-app {
        position: fixed;
        top: 20px;
        right: 20px;
        z-index: 1002;
        padding: 10px 20px;
        background: #281745;
        color: white;
        border: none;
        border-radius: 25px;
        cursor: pointer;
        font-weight: bold;
        text-decoration: none;
        transition: all 0.3s ease;
    }

        .back-to-app:hover {
            background: #281735;
            color: white;
            transform: translateY(-2px);
        }

    @@media (max-width: 768px) {
        .slide {
            padding: 30px;
            width: 95%;
            height: 85vh;
        }

        .slide-title {
            font-size: 2rem;
        }

        .content-grid {
            grid-template-columns: 1fr;
            gap: 20px;
        }

        .tech-stack {
            flex-direction: column;
            gap: 15px;
        }

        .flow-diagram {
            flex-direction: column;
            gap: 15px;
        }
    }
</style>

<div class="presentation-container">
    <a href="/asset-transfer" class="back-to-app">← 回到系統</a>

    <!-- Slide 1: 標題頁 -->
    <div class="slide active">
        <div class="logo">HCP PHOTONICS</div>
        <div class="slide-number">@currentSlide / @totalSlides</div>
        <div style="text-align: center; margin-top: 100px;">
            <h1 style="font-size: 3.5rem; color: #667eea; margin-bottom: 20px;">資產移轉系統</h1>
            <h2 style="font-size: 1.8rem; color: #666; margin-bottom: 40px;">Asset Transfer Management System</h2>
            <div style="font-size: 1.3rem; color: #888; margin-bottom: 60px;">
                保管資產的批次移交功能實作
            </div>
            <div class="tech-stack" style="justify-content: center; gap: 60px;">
                <div class="tech-item">
                    <div class="tech-icon">⚡</div>
                    <div>Blazor WebAssembly</div>
                </div>
                <div class="tech-item">
                    <div class="tech-icon">🔧</div>
                    <div>ASP.NET Core</div>
                </div>
                <div class="tech-item">
                    <div class="tech-icon">🗄️</div>
                    <div>SQL Server</div>
                </div>
            </div>
            <div style="margin-top: 60px; font-size: 1.1rem; color: #667eea;">
                開發者：[您的姓名] | 日期：@DateTime.Now.ToString("yyyy年MM月")
            </div>
        </div>
    </div>

    <!-- Slide 2: 系統概述 -->
    <div class="slide">
        <div class="logo">HCP PHOTONICS</div>
        <div class="slide-number">@currentSlide / @totalSlides</div>
        <div class="slide-header">
            <h1 class="slide-title">系統概述</h1>
            <p class="slide-subtitle">System Overview</p>
        </div>

        <div class="content-grid">
            <div class="feature-card">
                <h3 class="feature-title">🎯 業務需求</h3>
                <ul class="feature-list">
                    <li>員工轉調部門時的資產移交</li>
                    <li>批次處理多項資產轉移</li>
                    <li>支援一對多的移轉模式</li>
                    <li>完整的移轉確認流程</li>
                </ul>
            </div>

            <div class="feature-card">
                <h3 class="feature-title">⚙️ 核心功能</h3>
                <ul class="feature-list">
                    <li>保管人資產查詢</li>
                    <li>接收人指定與驗證</li>
                    <li>Excel確認單匯出</li>
                    <li>批次資產移轉執行</li>
                </ul>
            </div>
        </div>

        <div class="flow-diagram">
            <div class="flow-step">
                <div style="font-size: 2rem; margin-bottom: 5px;">🔍</div>
                <div>查詢保管人</div>
                <div style="font-size: 0.8rem; color: #666;">資產清單</div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-step">
                <div style="font-size: 2rem; margin-bottom: 5px;">📋</div>
                <div>匯出確認單</div>
                <div style="font-size: 0.8rem; color: #666;">Excel文件</div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-step">
                <div style="font-size: 2rem; margin-bottom: 5px;">✍️</div>
                <div>實體交接</div>
                <div style="font-size: 0.8rem; color: #666;">簽名確認</div>
            </div>
            <div class="flow-arrow">→</div>
            <div class="flow-step">
                <div style="font-size: 2rem; margin-bottom: 5px;">💾</div>
                <div>系統更新</div>
                <div style="font-size: 0.8rem; color: #666;">移轉完成</div>
            </div>
        </div>
    </div>

    <!-- Slide 3: 系統演示 -->
    <div class="slide">
        <div class="logo">HCP PHOTONICS</div>
        <div class="slide-number">@currentSlide / @totalSlides</div>
        <div class="slide-header">
            <h1 class="slide-title">系統展示</h1>
            <p class="slide-subtitle">Live System Demo</p>
        </div>

        <div style="text-align: center; margin: 50px 0;">
            <div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 40px; border-radius: 15px;">
                <h2 style="margin-bottom: 30px;">🎯 現場系統演示</h2>
                <p style="font-size: 1.2rem; margin-bottom: 30px;">
                    接下來將展示完整的資產移轉流程操作
                </p>
                <a href="/asset-transfer"
                   style="display: inline-block; background: white; color: #667eea; padding: 15px 30px; border-radius: 25px; text-decoration: none; font-weight: bold; font-size: 1.1rem; transition: all 0.3s ease;"
                   onmouseover="this.style.transform='translateY(-3px)'; this.style.boxShadow='0 10px 20px rgba(0,0,0,0.2)'"
                   onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='none'">
                    🚀 開始系統展示
                </a>
            </div>
        </div>

        <div class="content-grid">
            <div class="feature-card">
                <h3 class="feature-title">📋 展示重點</h3>
                <ul class="feature-list">
                    <li>員工搜尋與自動完成功能</li>
                    <li>資產查詢與預設全選</li>
                    <li>接收人指定與驗證</li>
                    <li>Excel確認單匯出</li>
                    <li>批次移轉執行</li>
                </ul>
            </div>

            <div class="feature-card">
                <h3 class="feature-title">🎯 技術特色</h3>
                <ul class="feature-list">
                    <li>響應式設計支援手機</li>
                    <li>即時資料驗證</li>
                    <li>分頁查詢大量資料</li>
                    <li>狀態管理與同步</li>
                    <li>友善的使用者介面</li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Slide 4: 總結 -->
    <div class="slide">
        <div class="logo">HCP PHOTONICS</div>
        <div class="slide-number">@currentSlide / @totalSlides</div>
        <div class="slide-header">
            <h1 class="slide-title">總結</h1>
            <p class="slide-subtitle">Summary</p>
        </div>

        <div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 30px; border-radius: 15px; text-align: center; margin-bottom: 30px;">
            <h3 style="margin-bottom: 20px; font-size: 1.8rem;">🎯 項目成果</h3>
            <div style="display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin: 25px 0;">
                <div>
                    <div style="font-size: 2.5rem; margin-bottom: 5px;">100%</div>
                    <div style="font-size: 0.9rem;">需求實現度</div>
                </div>
                <div>
                    <div style="font-size: 2.5rem; margin-bottom: 5px;">8+</div>
                    <div style="font-size: 0.9rem;">核心功能</div>
                </div>
                <div>
                    <div style="font-size: 2.5rem; margin-bottom: 5px;">3層</div>
                    <div style="font-size: 0.9rem;">系統架構</div>
                </div>
                <div>
                    <div style="font-size: 2.5rem; margin-bottom: 5px;">多重</div>
                    <div style="font-size: 0.9rem;">安全驗證</div>
                </div>
            </div>
        </div>

        <div class="content-grid">
            <div class="feature-card" style="background: #e8f5e8;">
                <h3 style="color: #28a745; margin-bottom: 15px;">✅ 已完成功能</h3>
                <ul class="feature-list">
                    <li>員工搜尋與自動完成</li>
                    <li>資產查詢與分頁展示</li>
                    <li>預設全選與手動選擇</li>
                    <li>接收人指定與驗證</li>
                    <li>Excel確認單匯出</li>
                    <li>批次資產移轉執行</li>
                    <li>響應式使用者介面</li>
                </ul>
            </div>

            <div class="feature-card" style="background: #fff3e0;">
                <h3 style="color: #ff9800; margin-bottom: 15px;">🚀 技術亮點</h3>
                <ul class="feature-list">
                    <li>Blazor WebAssembly前端</li>
                    <li>ASP.NET Core後端API</li>
                    <li>SQL Server資料庫</li>
                    <li>響應式設計支援</li>
                    <li>即時資料驗證</li>
                    <li>交易一致性保證</li>
                    <li>完整錯誤處理</li>
                </ul>
            </div>
        </div>

        <div style="text-align: center; margin-top: 40px;">
            <h3 style="color: #667eea; margin-bottom: 15px;">謝謝聆聽！</h3>
            <p style="font-size: 1.1rem; color: #666;">歡迎提問與討論</p>
        </div>
    </div>
</div>

<div class="navigation">
    <button class="nav-btn" @onclick="PreviousSlide" disabled="@(!CanPrevious)">◀ 上一頁</button>
    <button class="nav-btn" @onclick="NextSlide" disabled="@(!CanNext)">下一頁 ▶</button>
    <button class="nav-btn" @onclick="() => GoToSlide(1)">回到首頁</button>
</div>

@code {
    private int currentSlide = 1;
    private int totalSlides = 4;

    private bool CanPrevious => currentSlide > 1;
    private bool CanNext => currentSlide < totalSlides;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await JS.InvokeVoidAsync("eval", @"
                document.addEventListener('keydown', function(event) {
                    switch(event.key) {
                        case 'ArrowRight':
                        case ' ':
                            event.preventDefault();
                            DotNet.invokeMethodAsync('FrontEnd', 'NextSlideFromJS');
                            break;
                        case 'ArrowLeft':
                            event.preventDefault();
                            DotNet.invokeMethodAsync('FrontEnd', 'PreviousSlideFromJS');
                            break;
                    }
                });
            ");
        }
    }

    private void NextSlide()
    {
        if (CanNext)
        {
            var currentSlideElement = $".slide:nth-child({currentSlide})";
            var nextSlideElement = $".slide:nth-child({currentSlide + 1})";

            currentSlide++;
            UpdateSlideDisplay();
        }
    }

    private void PreviousSlide()
    {
        if (CanPrevious)
        {
            currentSlide--;
            UpdateSlideDisplay();
        }
    }

    private void GoToSlide(int slideNumber)
    {
        if (slideNumber >= 1 && slideNumber <= totalSlides)
        {
            currentSlide = slideNumber;
            UpdateSlideDisplay();
        }
    }

    private void UpdateSlideDisplay()
    {
        StateHasChanged();

        JS.InvokeVoidAsync("eval", $@"
            document.querySelectorAll('.slide').forEach((slide, index) => {{
                slide.classList.remove('active');
                if (index === {currentSlide - 1}) {{
                    slide.classList.add('active');
                }}
            }});
            document.querySelectorAll('.slide-number').forEach(num => {{
                num.textContent = '{currentSlide} / {totalSlides}';
            }});
        ");
    }

    [JSInvokable]
    public static void NextSlideFromJS()
    {
        // 這裡需要通過IJSRuntime調用實例方法
    }

    [JSInvokable]
    public static void PreviousSlideFromJS()
    {
        // 這裡需要通過IJSRuntime調用實例方法
    }
}